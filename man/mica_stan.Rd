% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mica_stan.R
\name{mica_stan}
\alias{mica_stan}
\title{Bayesian MICA Completion with Stan}
\usage{
mica_stan(
  corr_matrix,
  k_matrix = NULL,
  tau_matrix = NULL,
  chains = 4,
  iter = 1000,
  warmup = floor(iter/2),
  seed = 20260521,
  adapt_delta = 0.95,
  max_treedepth = 12,
  prior_prec_floor = 0.1,
  prior_prec_mult = 1,
  tau2_scale = 0.05,
  lkj_eta = 1,
  n_avg_per_study = 200,
  refresh = 0,
  ...
)
}
\arguments{
\item{corr_matrix}{A symmetric correlation matrix with ones on the diagonal
and \code{NA} entries for missing off-diagonal cells.}

\item{k_matrix}{Optional symmetric study-count matrix aligned to
\code{corr_matrix}.}

\item{tau_matrix}{Optional symmetric heterogeneity matrix aligned to
\code{corr_matrix}.}

\item{chains}{Number of Stan chains.}

\item{iter}{Number of post-warmup iterations per chain.}

\item{warmup}{Number of warmup iterations per chain.}

\item{seed}{Random seed passed to Stan.}

\item{adapt_delta}{Stan \code{adapt_delta}.}

\item{max_treedepth}{Stan \code{max_treedepth}.}

\item{prior_prec_floor}{Minimum prior precision on the Fisher-z scale.}

\item{prior_prec_mult}{Multiplier applied to the regression-bound prior
precision.}

\item{tau2_scale}{Scale parameter for the half-Cauchy prior on \code{tau}.}

\item{lkj_eta}{LKJ concentration parameter.}

\item{n_avg_per_study}{Average per-study sample size used when only \code{k}
counts are available.}

\item{refresh}{Stan refresh interval.}

\item{...}{Additional arguments passed to \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}.}
}
\value{
An object of class \code{"mica_stan"}.
}
\description{
Compatibility wrapper for users who want to explicitly request the Stan
engine. Internally this forwards to \code{mica(..., engine = "stan")}.
}
\examples{
\donttest{
toy <- matrix(c(
  1.00, 0.32,   NA, 0.21,
  0.32, 1.00, 0.28,   NA,
    NA, 0.28, 1.00, 0.41,
  0.21,   NA, 0.41, 1.00
), nrow = 4, byrow = TRUE)

fit <- mica_stan(toy, chains = 1, iter = 200, warmup = 100, refresh = 0)
fit
}
}
